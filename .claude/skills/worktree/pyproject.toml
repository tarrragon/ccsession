[project]
name = "worktree-skill"
version = "1.0.0"
description = "Git Worktree Management SKILL for Claude Code"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
]
