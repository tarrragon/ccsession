# v0.2.1 工作日誌

status: completed

## 版本目標

Flutter Frontend 基本連線：Flutter app 能連上 Go Backend，顯示 session 列表，點進去看到即時對話內容。最小 UI，不需要任何介面優化。

## 版本概要

| 項目 | 說明 |
|------|------|
| 版本號 | 0.2.1 |
| 開始日期 | 待定（v0.2.0 完成後） |
| 完成日期 | 2026-03-29 |
| Ticket 數量 | 30 |
| Wave 數量 | 2 |

## 版本里程碑

打開 Flutter app：
- 看到本機正在進行的 Claude Code session 列表（session ID、狀態、最後活動時間）
- 點進某個 session，看到即時滾動的對話內容（user 訊息、assistant 回應、tool_use）
- session 狀態變化（active → idle → completed）即時反映在列表

## 前置條件

- v0.2.0 完成（Go Backend 可正常運作）
- Backend WebSocket 端點 `ws://localhost:8765/ws` 穩定

## Wave 規劃

### Wave 1: Flutter Core

WebSocket 客戶端和基本 UI 骨架。

| Ticket | 標題 | 說明 |
|--------|------|------|
| W1-001 | Flutter WebSocket 客戶端 | 建立 WebSocket 連線、訊息解析、基本連線狀態管理 |
| W1-002 | Session 列表頁面 | 顯示所有 session，包含狀態標籤和基本 metadata |

**Wave 1 產出**：app 可連線並看到 session 列表

### Wave 2: 對話詳情

Session 對話內容渲染。

| Ticket | 標題 | 說明 |
|--------|------|------|
| W2-001 | 對話詳情頁面 | 訂閱指定 session，渲染 user/assistant/tool_use 訊息，即時追加新訊息 |
| W2-002 | 歷史訊息載入 | `get_session_history` 實作，首次開啟載入最新 100 筆，支援向上捲動載入更早訊息 |

**Wave 2 產出**：完整的 MVP —— 能看到 session 列表和對話內容

## v0.3 前置預留要求（來源：0.2.0-W5-001 分析）

> v0.3 Agent Graph 需要 `Stream<AgentMessage>` 驅動動畫。以下預留要求確保 v0.2.1 的架構設計不阻塞 v0.3 接入。

### W1-001 設計約束（強制）

| # | 預留要求 | 說明 |
|---|---------|------|
| 1 | WebSocket 事件層以 `Stream<SessionEvent>` 形式暴露 | v0.3 可透過 `.where().map()` 過濾為 `Stream<AgentMessage>`，零改動接入 |
| 2 | 定義 `SessionEventRepository` 抽象介面 | `Stream<SessionEvent> get events` + `subscribe/unsubscribe`，解耦元件與 WebSocket 實作 |
| 3 | WebSocket 訊息解析包含 `default` 分支 | 處理未知事件類型（v0.3 新增 `agent_status_change`、`agent_message`），避免 runtime 例外 |

### 架構選型建議

| 選項 | v0.3 接入難度 | 說明 |
|------|-------------|------|
| Riverpod `StreamProvider` | 低 | `.stream` 屬性天然相容 |
| `StreamController<SessionEvent>` | 低 | 直接暴露 `.stream` |
| `ChangeNotifier` / `ValueNotifier` | 高 | 需額外橋接層，不建議 |

### SessionEvent Dart 模型預留欄位

```dart
class SessionEvent {
  // 現有欄位...
  final String? agentId;  // v0.3 預留：產生此事件的 agent ID（v0.2 多數為 null）
}
```

## 版本範圍（不包含）

- Split View 多面板（v0.3.x）
- 搜尋功能（v0.3.x）
- UI 主題 / 樣式優化（v0.3.x）
- iOS / Android 支援（後續）
- 錯誤恢復 / 重連（v0.2.2）

## 開發流程

| Phase | 狀態 | 說明 |
|-------|------|------|
| Phase 0 | completed | SA 前置審查（v0.2.0 架構延續，無需重新審查） |
| Phase 1 | completed | 功能設計（各 Ticket 獨立完成） |
| Phase 2 | completed | 測試設計（各 Ticket 獨立完成） |
| Phase 3 | completed | 實作執行（30/30 Ticket 完成，434 Flutter + Go 測試全通過） |
| Phase 4 | completed | 重構評估（各 Ticket 獨立完成 Phase 4 評估） |

## 主要成果

### Wave 1: 基礎功能 + 品質強化（8 Tickets）
- 端對端整合測試套件（Go 9 + Flutter 11 = 20 個測試案例）
- Markdown 渲染支援
- 可觀測性規則納入品質基線
- Hook 降級和 PM 規則補充

### Wave 2: UI 體驗改善（5 Tickets）
- Session List 按專案分組頁籤
- Session 分頁機制 + Completed 預設摺疊
- Session 標題顯示改善（最新對話摘要 + Agent 標記）
- Conversation View 使用者 prompt 顯示修復

### Wave 3: 品質審查修復（3 Tickets）
- 多視角審查 P1/P2 問題修復
- 規則系統架構優化
- Bubble Widget 常數化

### Wave 4: 穩定性修復（6 Tickets）
- UI 排版設計規範建立
- Session 排序修正
- 多個顯示問題修復（氣泡無文字、時間戳、分組不顯示、即時更新）

### Wave 5: 效能與穩定性（5 Tickets）
- Agent Session 識別方案分析
- 記憶體洩漏修復（70GB → 正常）
- subscription 上限 + dedup 安全閥

