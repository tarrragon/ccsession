---
id: 0.1.1-W9-001.1
title: 統一 get_project_root() — 消除 CLAUDE.md 搜尋 vs git rev-parse 雙重實作
type: IMP
status: in_progress
version: 0.1.1
wave: 9
priority: P0
parent_id: 0.1.1-W9-001
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
who:
  current: thyme-python-developer
  history: {}
what: hook_utils/hook_base.py 和 ticket_system/lib/paths.py 的 get_project_root() 統一為
  git rev-parse --show-toplevel 優先策略
when: 待定義
where:
  layer: 待定義
  files:
  - .claude/hooks/hook_utils/hook_base.py
  - .claude/skills/ticket/ticket_system/lib/paths.py
why: worktree 環境下 CLAUDE.md 搜尋策略失效，導致所有路徑計算錯誤
how:
  task_type: Implementation
  strategy: 待定義
acceptance:
- '[ ] hook_base.py get_project_root() 使用 git rev-parse --show-toplevel 優先;paths.py
  get_project_root() 使用相同策略;worktree 環境下路徑解析正確;所有既有測試通過'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
decision_tree_path:
  entry_point: 第四層 Ticket 執行
  final_decision: 派發 thyme-python-developer 完整 TDD
  rationale: 統一 get_project_root() 為 git-native 策略，worktree 環境必要前置修復
assigned: true
creation_accepted: true
started_at: '2026-03-17T23:19:00'
completed_at: null
created: '2026-03-14'
updated: '2026-03-14'
---

# Execution Log

## Task Summary

hook_utils/hook_base.py 和 ticket_system/lib/paths.py 的 get_project_root() 統一為 git rev-parse --show-toplevel 優先策略

---

## Creation Review（建立品質審核）

**審核者**: acceptance-auditor
**審核時間**: 2026-03-17
**審核類型**: creation review（建立後品質審核，非完成驗收）

### 檢查結果摘要

| 檢查項目 | 結果 | 說明 |
|---------|------|------|
| 結構完整性（YAML 必填欄位） | PASS | 所有必填欄位已填寫，`when`/`where.layer` 為可選欄位標記待定義 |
| where.files 完整性 | PASS | 2 個目標檔案路徑均已完整列出，且使用完整路徑格式 |
| 驗收條件 4V 合規性 | PASS（附建議） | 條件內容合規，但格式有改善空間（見下方） |
| 數字正確性 | PASS | 無魔法數字，無需特別核查 |
| 後續任務銜接 | SKIP | IMP 類型，無需後續任務 |
| 子任務完成狀態 | SKIP | children 為空陣列 |

### 缺陷詳細記錄

**[中缺陷 - 格式] 驗收條件未拆分為獨立項目**

4 個驗收條件合併為 1 個字串（分號分隔）：
```
hook_base.py get_project_root() 使用 git rev-parse --show-toplevel 優先;
paths.py get_project_root() 使用相同策略;
worktree 環境下路徑解析正確;
所有既有測試通過
```

建議拆分為 4 個獨立的 acceptance 陣列項目，以便逐條勾選追蹤。

判定：條件**內容**均符合 4V 標準（Verifiable/Valuable/Version-specific/Viable），缺陷為格式而非內容，不影響執行品質。

**[低缺陷] "worktree 環境下路徑解析正確" 可操作性不足**

此條件未指定具體驗證方式（例如：在 worktree 環境下執行 `get_project_root()`，預期回傳專案根目錄而非 worktree 子目錄）。執行代理人需自行定義驗證步驟。

### 審核結論

**判定結果**: 通過（附改善建議）

理由：
1. 高缺陷數：0
2. 中缺陷數：1（格式問題，條件內容本身合規）
3. 低缺陷數：1
4. 此為已審核父任務（0.1.1-W9-001）的子任務，範圍已確認

`creation_accepted` 已設為 `true`，可認領執行。

**改善建議（非阻塞）**：執行代理人在開始前，建議將驗收條件拆分為 4 個獨立項目並補充 worktree 驗證的具體步驟，以便完成時逐條勾選。

---

## Problem Analysis

<!-- 建立者可在此記錄調查過程，範例：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置（已確認）：
- file1.py:123
確認的位置（推測，待接手者驗證）：
- file2.py:456（未逐一確認）
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

Phase 1 完成：功能規格設計文件建立於 docs/work-logs/v0.1.1/tickets/0.1.1-W9-001.1-phase1-design.md。核心設計：git rev-parse 優先策略（優先級：環境變數 > git rev-parse > CLAUDE.md 搜尋 > cwd fallback）。回傳型別維持 Path，兩個檔案獨立實作，22 個 hook 和 3 個 ticket 命令自動適配。定義 14 條驗收標準（AC-1 至 AC-14）和 7 個 Given-When-Then 場景。
## Solution

<!-- To be filled by executing agent -->

---

## Test Results

<!-- To be filled by executing agent -->

---

## Phase 2 測試設計 完成

### 產出物
- ✓ 測試設計文件：docs/work-logs/v0.1.1/tickets/0.1.1-W9-001.1-phase2-test-design.md
- ✓ git commit：feat(0.1.1-W9-001.1): Phase 2 完成

### 測試案例統計
- **總計**：24 個測試案例
  - hook_base.py：11 個單元測試
  - paths.py：9 個單元測試  
  - 整合測試：4 個對稱性驗證

### 設計覆蓋
- ✓ 環境變數優先級（場景 1, A1）
- ✓ git rev-parse 優先級（場景 2, A2）- **worktree 修復場景**
- ✓ CLAUDE.md 搜尋 fallback（場景 7, A4）
- ✓ cwd 最終 fallback（場景 9, A8）
- ✓ git 異常處理（場景 4-6, A4-A6）
  - FileNotFoundError（git 不存在）
  - TimeoutExpired（git 超時）
  - returncode != 0（非 git repo）
- ✓ 邊界條件（8 個）
- ✓ 向後相容性（現有呼叫端無需修改）
- ✓ 兩檔案對稱性（整合測試）

### 驗收標準映射
所有 14 個功能驗收標準（AC-1 至 AC-14）均有對應測試：
- AC-1, AC-2：git rev-parse 優先（場景 2, A2）
- AC-3：worktree 修復（場景 3, A3）
- AC-4, AC-5：異常處理（場景 4-6, A4-A6）
- AC-6, AC-7, AC-8：向後相容性（場景 11, A8）
- AC-9-AC-11：測試覆蓋驗證
- AC-12：對稱性測試（整合測試 4 個）
- AC-13：GIT_TOPLEVEL_TIMEOUT 常數
- AC-14：pytest 100% 通過

### 測試設計要點
- **行為鏈式推演**：Step 1-4（前置驗證→行為觸發→結果確認→狀態驗證）
- **四維度分支窮舉**：正常/異常/邊界/中斷操作全覆蓋
- **Mock 策略**：Sociable Unit Tests（only mock 外部依賴）
  - subprocess.run（git 命令）
  - os.environ（環境變數）
  - Path.cwd()（當前目錄）
  - 真實檔案系統（tmp_path fixture）
- **完整的 fixture 設計**：mocK git repo, worktree, subprocess
- **詳細的測試資料準備**

### 測試檔案位置規劃
- .claude/hooks/tests/test_hook_base_get_project_root.py
- .claude/skills/ticket/tests/test_paths_get_project_root.py
- .claude/hooks/tests/test_project_root_symmetry.py

### 預期測試執行（Phase 3b）


預期結果：24/24 (100%) 通過，測試覆蓋率 100%

### Handoff 到 Phase 3a
-> pepper-test-implementer：將測試設計轉換為語言無關的實作策略和虛擬碼

---

## Phase 3a 實作策略規劃 完成

### 產出物
- ✓ 策略設計文件：docs/work-logs/v0.1.1/tickets/0.1.1-W9-001.1-phase3a-strategy.md
- ✓ git commit：feat(0.1.1-W9-001.1): Phase 3a 完成 - get_project_root 實作策略規劃

### 策略設計覆蓋

**1. 實作策略設計**
- ✓ 4 層優先順序虛擬碼完整（環境變數 > git rev-parse > CLAUDE.md/marker 搜尋 > cwd fallback）
- ✓ 異常處理策略明確：3 種異常類型捕捉（FileNotFoundError、TimeoutExpired、returncode != 0）
- ✓ 兩個檔案對稱性設計定義：
  - 完全相同：環境變數、git rev-parse、層級 4 fallback
  - 故意不同：層級 3 fallback（hook_base.py 有深度限制，paths.py 無）

**2. 資料流程圖和控制流程圖**
- ✓ 資料流程圖：展示環境變數 → git 結果 → 搜尋結果 → cwd 的轉換路徑
- ✓ 控制流程圖：12 個決策點和異常分支的完整流程

**3. 資料結構選擇**
- ✓ Path 型別維持現有簽名（無需修改呼叫端）
- ✓ subprocess.CompletedProcess 用於 git 命令結果
- ✓ GIT_TOPLEVEL_TIMEOUT = 5 秒新增常數

**4. 實作指引**
- ✓ 第一階段目標（hook_base.py）：環境變數優先 → git rev-parse 新增 → CLAUDE.md 搜尋 fallback
- ✓ 第二階段目標（paths.py）：相同邏輯，保留 marker 多檔案搜尋
- ✓ 兩個檔案獨立實作，無需共用函式（避免循環依賴）

**5. Python 語言特定考量**
- ✓ subprocess.run() 用法（capture_output=True, text=True, timeout=5）
- ✓ pathlib.Path 跨平台路徑處理
- ✓ 異常類型分層（OSError → FileNotFoundError、subprocess.TimeoutExpired）
- ✓ stdout.strip() 移除尾部換行符
- ✓ Windows/symlink 邊界條件處理

**6. 風險識別和緩修**
- ✓ 實作風險 5 項（git rev-parse 邏輯遺漏、優先級搞反、returncode 判斷、異常遺漏、stdout 處理）
- ✓ 測試風險 4 項（mock 不準確、worktree 複雜、環境變數隔離、對稱性驗證）
- ✓ 相容性風險 3 項（22 個 hook、3 個 ticket 命令、向後相容性）
- ✓ 所有風險都有明確的緩修措施

**7. 驗收標準映射**
- ✓ AC-Strategy-1：4 層優先順序虛擬碼完整
- ✓ AC-Strategy-2：流程圖已繪製
- ✓ AC-Strategy-3：異常處理策略明確
- ✓ AC-Strategy-4：兩檔案對稱性設計已定義
- ✓ AC-Strategy-5：實作檢查清單完整
- ✓ AC-Strategy-6：Python 語言考量列舉
- ✓ AC-Strategy-7：風險識別和緩修已規劃

### 執行時間估算
- 資源設計：20 分鐘
- 虛擬碼撰寫：30 分鐘
- 流程圖繪製：20 分鐘
- 實作指引編寫：30 分鐘
- 風險識別分析：20 分鐘
- **總計**：120 分鐘（2 小時）

### 策略文件結構
1. 實作策略設計（4 層優先順序、虛擬碼、異常處理）
2. 資料結構選擇（Path、subprocess、常數定義）
3. 資料流程圖（10 個轉換節點）
4. 控制流程圖（12 個決策和分支）
5. 關鍵實作指引（兩個階段、實作要點）
6. 權宜方案與技術債務（3 項）
7. Python 特定考量（5 個維度）
8. 實作檢查清單（20 項檢查點）
9. 風險識別（12 項風險 + 緩修）
10. 交接清單（4 個需求）
11. 執行時間估算
12. 職責邊界說明

### Handoff 到 Phase 3b
-> parsley-flutter-developer：依策略實作 Python 代碼，執行 24 個測試案例，驗證所有驗收標準

## Completion Info

**Completion Time**: 2026-03-17T23:26:00
**Executing Agent**: pepper-test-implementer
**Review Status**: awaiting phase3b execution