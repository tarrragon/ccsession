---
id: 0.1.1-W13-002.1
title: 修復 worktree SKILL 剩餘品質問題 — git_utils.py H5 + M6/M8/M9/M10/M11/M12
type: IMP
status: in_progress
version: 0.1.1
wave: 13
priority: P1
parent_id: 0.1.1-W13-002
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who: thyme-python-developer
what: 修復 worktree SKILL 剩餘品質問題 — git_utils.py H5 + M6/M8/M9/M10/M11/M12
when: 待定義
where:
  layer: ".claude/lib (git 工具層), .claude/skills/worktree (業務層), tests (測試層)"
  files:
    - .claude/lib/git_utils.py
    - .claude/skills/worktree/scripts/worktree_manager.py
    - .claude/skills/worktree/scripts/constants.py
    - .claude/skills/worktree/tests/conftest.py
    - .claude/skills/worktree/tests/test_worktree_status.py
why: worktree SKILL 品質改善階段 - 消除低優先級品質債（H5 高優先級異常處理+6 個中優先級設計改善）
how:
  task_type: Implementation
  strategy: "逐步修復 7 個品質問題：(1) M9 搬移函式避免行號變化 (2) H5 加入雙通道異常處理 (3-7) 其他 6 個優化。完成後執行測試確認 61/61 通過，然後 commit。"
acceptance:
- '[ ] 任務實作完成'
- '[ ] 相關測試通過'
- '[ ] 無程式碼品質警告'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: true
started_at: '2026-03-21T09:02:44'
completed_at: null
created: '2026-03-21'
updated: '2026-03-21'
---

# Execution Log

## Task Summary

修復 worktree SKILL 剩餘品質問題 — git_utils.py H5 + M6/M8/M9/M10/M11/M12

---

## Problem Analysis

### 父任務進度
- 父任務 0.1.1-W13-002 已完成 10/19 個修復
- 當前子任務負責剩餘 7 個品質問題

### H5 [高優先級] git_utils.py except Exception 靜默吞掉
**位置**:
- `.claude/lib/git_utils.py` 行 89: `run_git_command()` 的 except Exception
- `.claude/lib/git_utils.py` 行 246: `is_in_worktree()` 的 except Exception

**問題**: 未預期的例外未寫入 stderr，違反 quality-baseline 規則 4 雙通道要求

### M6 [中優先級] 測試檔案硬編碼絕對路徑
**位置**: `.claude/skills/worktree/tests/` 目錄
- conftest.py 行 29-31, 47：mock 返回值中硬編碼 `/Users/mac-eric/...`
- test_worktree_status.py 行 27, 45-48, 71-72, 90-91：測試用例硬編碼路徑

**問題**: 絕對路徑依賴於特定開發環境，影響可移植性

### M8 [中優先級] TICKET_ID_PATTERN 在 worktree/ticket 各自定義
**位置**:
- `.claude/skills/worktree/scripts/constants.py` 行 50
- `.claude/skills/ticket/ticket_system/lib/constants.py`（源）

**問題**: 重複定義同一個正則表達式，違反 DRY 原則

### M9 [中優先級] generate_worktree_info 是 UI 邏輯混入 git_utils
**位置**: `.claude/lib/git_utils.py` 行 252-268

**問題**: 格式化文字輸出的 UI 邏輯不應在工具層，應搬移到業務層

### M10 [中優先級] git branch --merged 對每個 worktree 重複執行
**位置**: `.claude/skills/worktree/scripts/worktree_manager.py`
- `_cleanup_classify_worktrees()` 中的迴圈多次呼叫 `_cleanup_check_level3()`
- 每次呼叫都執行 `git branch --merged <base>`

**問題**: N 個 worktree 時執行 N 次相同命令，效能低下

### M11 [中優先級] get_worktree_ahead_behind 可用 rev-list --left-right 合併
**位置**: `.claude/skills/worktree/scripts/worktree_manager.py` 行 182-213

**問題**: 兩次 `git rev-list --count` 呼叫可合併為一次 `--left-right` 呼叫

### M12 [中優先級] _find_worktree 薄包裝 + 重複 git worktree list
**位置**: `.claude/skills/worktree/scripts/worktree_manager.py` 行 710-720

**問題**: `_find_worktree()` 只是 `_find_target_worktree()` 的薄包裝，無增加值

### 修復順序（重要）
1. **M9 優先** - generate_worktree_info 搬移（影響行號）
2. **H5** - except Exception 雙通道
3. **M6** - 測試硬編碼路徑
4. **M8** - TICKET_ID_PATTERN 統一
5. **M10** - git branch --merged 快取
6. **M11** - rev-list 合併
7. **M12** - 移除薄包裝

---

## Solution

<!-- To be filled by executing agent -->

---

## Test Results

<!-- To be filled by executing agent -->

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending