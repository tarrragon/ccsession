---
id: 0.1.1-W18-001-pm-handoff
title: Ticket 0.1.1-W18-001 PM 交接文件 - 已準備派發 Phase 3b 執行
created: 2026-03-22
---

# PM 交接文件：Ticket 0.1.1-W18-001

## 概況

**Ticket ID**: 0.1.1-W18-001
**標題**: 重構 scan_skill_packages 和 scan_custom_packages 跨模組 pyproject.toml 掃描邏輯重複
**優先級**: P2
**波次**: W18
**狀態**: Phase 1-3a 完成，已準備派發 Phase 3b 執行

## PM 已完成工作（Phase 0-3a）

### Phase 1：功能規格設計 ✅

**產出物**: `0.1.1-W18-001-phase1-design.md`

**設計亮點**：
- 分析了 95 行重複代碼（pyproject.toml 解析 + 目錄遍歷）
- 決定提取共用模組 `.claude/lib/pyproject_scanner.py`
- 設計了 5 個公開 API 函數
- 考慮了 Hook 環境約束（無法直接 import project-init）
- 提供了環境相容性解決方案（sys.path.insert）

**核心決定**：
- 方案 B：共用模組（vs 各自保留 / 復制粘貼）
- 返回格式統一為 Hook 格式 Dict[str, Dict[str, str]]
- 不在共用模組層實施快取（交由各端自行決定）

---

### Phase 2：測試設計 ✅

**產出物**: `0.1.1-W18-001-phase2-test-design.md`

**測試覆蓋範圍**：

| 項目 | 數量 | 說明 |
|------|------|------|
| 共用模組單元測試 | 17 | 完整覆蓋 5 個公開函數的成功/失敗路徑 |
| Hook 集成測試 | 4 | 驗證 Hook 能正確使用共用模組 |
| CLI 集成測試 | 5 | 驗證 check/setup 命令仍正常工作 |
| **總計** | **26** | 目標覆蓋率 >= 80% |

**測試資料**：提供了 fixture 範本（valid/invalid/minimal pyproject.toml）

**執行命令**：
```bash
(cd .claude/lib && uv run pytest tests/test_pyproject_scanner.py -v)
(cd .claude/hooks && uv run pytest tests/test_manual_verification.py -v)
(cd .claude/skills/project-init && uv run pytest tests/test_integration.py -v)
```

---

### Phase 3a：實作策略 ✅

**產出物**: `0.1.1-W18-001-phase3a-strategy.md`

**詳細執行計畫**（6 個步驟）：

1. **建立共用模組** (新建，~180 行)
   - 完整的虛擬碼
   - 品質要求（型別標註、docstring、長度 <= 30 行）

2. **修改 Hook** (刪除 30 行，新增 5 行)
   - 移除 `_load_pyproject_toml()`
   - 修改 `scan_skill_packages()` 調用共用模組
   - 新增 sys.path.insert 導入

3. **修改 CLI** (刪除 100 行，新增 15 行)
   - 移除 `_parse_pyproject()` 和三個提取函數
   - 修改 `scan_custom_packages()` 的格式轉換邏輯
   - 新增 import 路徑設定

4. **驗證編譯和語法** (py_compile 檢查)

5. **執行測試套件** (分步驟驗證)

6. **完成確認** (grep 驗證重複代碼已移除)

**風險評估**：
- 導入路徑錯誤（低）→ 詳細測試驗證
- 格式轉換邏輯錯誤（低）→ CLI 集成測試覆蓋
- Hook 執行環境問題（低）→ Hook 集成測試驗證

**預期變更統計**：
- 新增：180 行（共用模組）+ 20 行（導入邏輯）
- 刪除：130 行（重複代碼）
- 淨增：70 行
- **重複代碼消除**：95 行

---

## 派發指示

### 派發對象

**代理人**: thyme-python-developer（Python 開發專家）
**任務**: Phase 3b 實作執行 + Phase 4 重構評估

### 派發信息

**前置條件檢查清單**：
- [ ] 已閱讀 Phase 1 設計（確認理解方案）
- [ ] 已閱讀 Phase 2 測試設計（確認測試覆蓋）
- [ ] 已閱讀 Phase 3a 策略（確認執行步驟）
- [ ] Hook 和 CLI 已被識別為修改目標

**執行要求**：

1. **遵循 Phase 3a 虛擬碼精確實作**
   - 共用模組 `.claude/lib/pyproject_scanner.py`
   - Hook 修改 `.claude/hooks/package-version-sync-hook.py`
   - CLI 修改 `.claude/skills/project-init/project_init/lib/package_manager.py`

2. **品質標準**（遵循 quality-python.md）
   - 所有公開函數有型別標註和 docstring
   - 無硬編碼字符串（提取為常數）
   - 函式長度 <= 30 行
   - PEP 8 風格

3. **測試執行**
   - 執行 Phase 2 中設計的全部 26 個測試
   - 預期全部通過，無警告
   - 如有測試失敗，分析根因後修正（強制派發 incident-responder）

4. **驗證檢查**
   ```bash
   # 重複代碼驗證（應無匹配）
   grep -n "_load_pyproject_toml\|_parse_pyproject\|_extract_" \
     .claude/hooks/package-version-sync-hook.py \
     .claude/skills/project-init/project_init/lib/package_manager.py
   ```

5. **文件更新**
   - 更新 `0.1.1-W18-001.md` 執行日誌：
     - 新增 Phase 3b 執行結果
     - 新增 Phase 4 評估報告（或簡短評語）
   - 新增執行耗時統計

### 成功條件

| 條件 | 驗證方式 |
|------|---------|
| 共用模組建立完成 | 檔案存在且能被 import |
| Hook 修改完成 | py_compile 無錯誤，測試通過 |
| CLI 修改完成 | py_compile 無錯誤，測試通過 |
| 測試 100% 通過 | `uv run pytest` 輸出無失敗 |
| 重複代碼消除 | grep 驗證已移除 |
| 品質無警告 | 無未捕獲的異常或 linting 警告 |

### 失敗時流程

如果測試失敗：
1. 執行 `/pre-fix-eval` 進行錯誤分析
2. 派發 incident-responder 診斷問題
3. 建立修復 Ticket（可為子任務）
4. 修復後重新執行測試

如果代碼品質警告：
1. 修正命名、格式或結構
2. 確保符合 quality-python.md 所有要求
3. 重新驗證

---

## 相關文件清單

| 檔案 | 用途 |
|------|------|
| [0.1.1-W18-001.md](0.1.1-W18-001.md) | 原始 Ticket（主記錄） |
| [0.1.1-W18-001-phase1-design.md](0.1.1-W18-001-phase1-design.md) | Phase 1 功能規格設計 |
| [0.1.1-W18-001-phase2-test-design.md](0.1.1-W18-001-phase2-test-design.md) | Phase 2 測試設計規格 |
| [0.1.1-W18-001-phase3a-strategy.md](0.1.1-W18-001-phase3a-strategy.md) | Phase 3a 實作策略文件 |
| 本文件 | Phase 3b 派發交接說明 |

**技術參考文件**：
- [quality-python.md](../../../../.claude/rules/core/quality-python.md) - Python 品質規則
- [cognitive-load.md](../../../../.claude/rules/core/cognitive-load.md) - 認知負擔設計原則
- [tdd-flow.md](../../../../.claude/rules/flows/tdd-flow.md) - TDD 流程規範

---

## 快速檢查清單

**thyme-python-developer 執行前確認**：

- [ ] 理解設計方案（提取共用模組）
- [ ] 確認修改檔案位置（3 個檔案）
- [ ] 確認品質標準（type hints, docstring, max 30 lines）
- [ ] 確認測試目錄存在（.claude/lib/tests/, .claude/hooks/tests/, project-init/tests/）
- [ ] 確認可訪問 `.claude/lib/` 和 project-init 套件
- [ ] 確認開發環境正常（Python 3.9+, uv, pytest）

---

**交接時間**: 2026-03-22T07:00:00
**交接者**: rosemary-project-manager（主線程）
**派發狀態**: 已準備就緒，等待 thyme-python-developer 認領
