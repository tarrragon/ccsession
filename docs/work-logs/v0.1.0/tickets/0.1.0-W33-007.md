---
id: 0.1.0-W33-007
title: 修復 parse_ticket_date ISO Z 後綴相容 + logger Optional 型別統一
type: ADJ
status: completed
version: 0.1.0
wave: 33
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
who:
  current: thyme-python-developer
  history: {}
what: 1. parse_ticket_date 加上 Z 後綴處理（replace Z +00:00），防止 Python 3.9 fromisoformat
  解析失敗無聲退化 2. validate_ticket_has_decision_tree logger 型別改為 Optional 並加 if logger
  防護，與同模組其他函式一致
when: 待定義
where:
  layer: hooks
  files:
  - hook_utils.py
why: W31+W32 多視角審查（Quality 視角）：Z 後綴會導致 started_at 解析失敗退化為日期精度；logger 型別標記不一致誤導未來呼叫者
how:
  task_type: Implementation
  strategy: 待定義
acceptance:
- '[x] 任務實作完成'
- '[x] 相關測試通過'
- '[x] 無程式碼品質警告'
tdd_phase: phase2
tdd_stage:
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-03-10T11:17:07'
completed_at: '2026-03-10T11:21:31'
created: '2026-03-10'
updated: '2026-03-10'
---

# Execution Log

## Task Summary

1. parse_ticket_date 加上 Z 後綴處理（replace Z +00:00），防止 Python 3.9 fromisoformat 解析失敗無聲退化 2. validate_ticket_has_decision_tree logger 型別改為 Optional 並加 if logger 防護，與同模組其他函式一致

---

## Problem Analysis

<!-- 建立者可在此記錄調查過程，範例：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置（已確認）：
- file1.py:123
確認的位置（推測，待接手者驗證）：
- file2.py:456（未逐一確認）
注意：接手者應獨立重新驗證數量/範圍（PC-007）
-->

---

## Solution

### 修複內容（.claude/hooks/hook_utils.py）

#### 1. parse_ticket_date Z 後綴處理（第 701-710 行）

**問題**：Python 3.9 的 `datetime.fromisoformat()` 不支援 "Z" 後綴，導致 ISO 8601 格式日期（如 `2026-03-10T11:17:07Z`）解析失敗。

**解決方案**：
```python
# 優先級 1: ISO 8601 / RFC 3339（使用 fromisoformat）
# Python 3.9 不支援 "Z" 後綴，替換為 "+00:00"（RFC 3339 相容格式）
try:
    normalized_value = value.replace("Z", "+00:00") if value.endswith("Z") else value
    dt = datetime.fromisoformat(normalized_value)
```

- 在 `fromisoformat()` 呼叫前檢查字串是否以 'Z' 結尾
- 若以 'Z' 結尾，替換為 '+00:00'（RFC 3339 相容格式）
- 若不含 'Z' 則保持原值，無額外開銷

#### 2. validate_ticket_has_decision_tree logger 型別統一（第 1044-1071 行）

**問題**：logger 參數為必需（無預設值），型別標記為 `logging.Logger`，與同模組其他函式（如 `run_git`、`parse_ticket_frontmatter`）的 Optional 設計不一致。

**解決方案**：
```python
def validate_ticket_has_decision_tree(ticket_content: str, logger: "Optional[logging.Logger]" = None) -> bool:
    """驗證 Ticket 是否包含決策樹欄位...

    Args:
        ticket_content: Ticket 檔案內容
        logger: Logger 物件（可選）
    """
    if not ticket_content:
        if logger:
            logger.debug("Ticket 內容為空")
        return False

    # 檢查任何決策樹欄位...
    for marker in DECISION_TREE_MARKERS:
        if marker in ticket_content:
            if logger:
                logger.debug("在 Ticket 中找到決策樹標記: {}".format(marker))
            return True

    if logger:
        logger.debug("未在 Ticket 中找到決策樹欄位")
    return False
```

- 將 logger 型別改為 `Optional[logging.Logger]`，加預設值 `None`
- 所有 `logger.xxx()` 呼叫前加上 `if logger` 防護檢查
- 更新 docstring 為『Logger 物件（可選）』
- 與同模組其他函式的 logger 參數設計一致

---

## Test Results

### 測試驗證

執行了三組測試驗證修改的正確性：

#### Test 1: parse_ticket_date Z 後綴處理
```
Input: "2026-03-10T11:17:07Z"
Output: datetime(2026, 3, 10, 11, 17, 7, tzinfo=timezone.utc)
Status: PASS ✓
```

#### Test 2: parse_ticket_date +00:00 格式（向後相容）
```
Input: "2026-03-10T11:17:07+00:00"
Output: datetime(2026, 3, 10, 11, 17, 7, tzinfo=timezone.utc)
Status: PASS ✓
```

#### Test 3: parse_ticket_date 簡單日期格式
```
Input: "2026-03-10"
Output: datetime(2026, 3, 10, 0, 0, 0)
Status: PASS ✓
```

#### Test 4: validate_ticket_has_decision_tree 無 logger 參數（Optional）
```
Input: ticket_content 含 "decision_tree_path:", logger=None
Output: True（找到決策樹欄位）
Status: PASS ✓
```

#### Test 5: validate_ticket_has_decision_tree 有 logger 參數
```
Input: ticket_content 含 "decision_tree_path:", logger=logging.Logger
Output: True（找到決策樹欄位）
Status: PASS ✓
```

#### Test 6: validate_ticket_has_decision_tree 空內容
```
Input: "", logger=None
Output: False（無內容）
Status: PASS ✓
```

### 程式碼品質驗證
- ✓ 遵循 PEP 8 風格
- ✓ 無語法錯誤（Python 3.9 相容）
- ✓ 型別標註完整
- ✓ 文件字串清晰
- ✓ 向後相容性維持（現有呼叫無需修改）

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: thyme-python-developer
**Review Status**: pending