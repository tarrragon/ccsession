---
id: 0.1.0-W22-007
title: '實作 acceptance-gate-hook.py 場景 #17 錯誤學習改進追蹤邏輯：ticket-lifecycle.md v5.2.0 已定義規則，但
  Hook 實現與 hook_messages.py 訊息定義均尚未完成（規則超前實現 4+ 天）'
type: IMP
status: completed
version: 0.1.0
wave: 22
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo:
- 0.1.0-W22-009
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
who:
  current: pending
  history: {}
what: '實作 acceptance-gate-hook.py 場景 #17 錯誤學習改進追蹤邏輯：ticket-lifecycle.md v5.2.0 已定義規則，但
  Hook 實現與 hook_messages.py 訊息定義均尚未完成（規則超前實現 4+ 天）'
when: ticket track complete 命令執行時（PreToolUse），驗收條件檢查之後
where:
  layer: Hook 系統層（.claude/hooks/）
  files:
  - .claude/hooks/acceptance-gate-hook.py
  - .claude/hooks/lib/hook_messages.py
why: 'ticket-lifecycle.md v5.2.0 已完整定義場景 #17 規則，但 acceptance-gate-hook.py 與 hook_messages.py
  均缺少對應實現，形成「規則已發布、Hook 未實現」的缺口'
how:
  task_type: Implementation
  strategy: '1. acceptance-gate-hook.py：新增 error-pattern 時間比對邏輯（步驟 6.5），比對 ticket
    created vs .claude/error-patterns/ mtime；2. hook_messages.py：在 AskUserQuestionMessages
    新增場景 #17 三個選項訊息常數；3. 更新 generate_hook_output 以支援場景 #17 輸出'
acceptance:
- '[x] ticket track complete 執行時，若 .claude/error-patterns/ 有新增檔案（mtime > ticket created），acceptance-gate-hook.py
  正確輸出 AskUserQuestion #17 提醒'
- '[x] 未有新增 error-pattern 時，complete 流程正常通過，不觸發 #17 提醒'
- '[x] hook_messages.py 的 AskUserQuestionMessages 包含場景 #17 三個選項的訊息常數'
- '[x] 新增邏輯不破壞現有場景 #1（驗收方式確認）和 #2（complete 後下一步）的行為'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: true
started_at: '2026-03-08T20:24:39'
completed_at: '2026-03-08T22:24:23'
created: '2026-03-08'
updated: '2026-03-08'
current_phase: Phase 4
assignee: cinnamon-refactor-owl
---

# Execution Log

## Task Summary

實作 acceptance-gate-hook.py 場景 #17 錯誤學習改進追蹤邏輯：ticket-lifecycle.md v5.2.0 已定義規則，但 Hook 實現與 hook_messages.py 訊息定義均尚未完成（規則超前實現 4+ 天）

---

## Problem Analysis

**確認的現狀（SA + acceptance-auditor 雙重驗證）**：

1. `acceptance-gate-hook.py`：目前只有場景 #1（驗收方式確認）和 #2（complete 後下一步），無場景 #17 邏輯
   - 確認位置：`.claude/hooks/acceptance-gate-hook.py`（第 16-18 行說明區）
   - 現有輸出優先級：錯誤訊息 → 場景 #1 → 場景 #2

2. `hook_messages.py`：`AskUserQuestionMessages` 類別存在，但無場景 #17 對應訊息
   - 確認位置：`.claude/hooks/lib/hook_messages.py`（第 576 行起 `AskUserQuestionMessages` 類別）
   - 現有訊息：`COMPLETE_REMINDER`（場景 #1）、`COMPLETE_NEXT_STEP_REMINDER`（場景 #2）

**SA 審查結論**（saffron-system-analyst 2026-03-08）：
- 架構位置正確：在 acceptance-gate-hook.py 的步驟 6.5 插入 error-pattern 檢查
- 無重複實作：doc-sync-check-hook.py 的 error-patterns 掃描粒度不同（全域 vs 單一 Ticket 期間）
- 不需要 SA Phase 0（為既有定義功能的實作補完）

---

Phase 1 設計規格已完成（basil-hook-architect），詳見 docs/work-logs/v0.1.0/tickets/0.1.0-W22-007-phase1-design.md。設計涵蓋：get_ticket_created_time()、check_error_patterns_changed() 新增函式，check_acceptance_status() 擴展為 5-tuple，generate_hook_output() 新增場景 #17 輸出，AskUserQuestion #17 訊息常數 ERROR_PATTERN_REMINDER。
## Solution

Phase 4a 多視角分析完成（Redundancy + Coupling + Complexity + linux Good Taste）。Phase 4b 重構範圍（5 項）：1. Line 622 刪除 if not False and 死碼；2. check_acceptance_status() 5-tuple 改為 NamedTuple；3. RFC 3339 時間解析改用 datetime.fromisoformat()；4. check_acceptance_status() 拆分（107行/負擔25→3函式）；5. get_ticket_created_time() 提取日期解析（64行/負擔15）。技術債追蹤：W22-021（parse_ticket_frontmatter 統一）、W22-022（main() 重構）。
### Phase 3b 實作完成（parsley-flutter-developer）

已完成 acceptance-gate-hook.py 場景 #17 的完整實作，包括新增兩個輔助函式、修改三個核心函式、新增訊息常數。

#### 修改檔案清單

**1. `.claude/hooks/lib/hook_messages.py`**
- 新增 `ERROR_PATTERN_REMINDER` 訊息常數（行 821-857）
- 位置：`AskUserQuestionMessages` 類別內
- 內容：三個選項說明、檔案清單佔位符 `{file_list}`、規範連結

**2. `.claude/hooks/acceptance-gate-hook.py`**

**新增函式 1**: `get_ticket_created_time(ticket_file, logger) → Optional[datetime]`（行 423-485）
- 從 Ticket frontmatter 讀取 `created` 欄位並解析為 datetime
- 支援三種格式：ISO 8601、簡單日期 YYYY-MM-DD、RFC 3339 含時區
- 錯誤處理：返回 None，記錄 warning 日誌

**新增函式 2**: `check_error_patterns_changed(project_root, ticket_created, logger) → Tuple[bool, List[str]]`（行 488-545）
- 掃描 `.claude/error-patterns/` 目錄，找出所有 mtime > ticket_created 的 .md 檔案
- 返回值：(has_changed, file_list)
- 邊界處理：目錄不存在/時間無法解析/無讀取權限時優雅降級，返回 (False, [])

**修改函式 1**: `check_acceptance_status()` 簽名與邏輯（行 548 + 611-627）
- 返回值由 3-tuple 擴展為 5-tuple：`(should_block, has_acceptance, message, has_new_error_patterns, new_error_pattern_files)`
- 新增步驟 6.5：error-pattern 檢查邏輯（行 611-627）
- 條件限制：僅在不阻擋且需要驗收檢查時執行
- 所有返回語句都更新為 5-tuple（共 5 個地點）

**修改函式 2**: `generate_hook_output()` 簽名與優先級邏輯（行 650 + 668-685）
- 新參數：`has_new_error_patterns: bool = False`, `new_error_pattern_files: Optional[List[str]] = None`
- 新優先級邏輯：
  1. message（最高優先級）
  2. ERROR_PATTERN_REMINDER（場景 #17，條件：has_new_error_patterns=True AND message=None）
  3. COMPLETE_REMINDER（場景 #1，保留，條件：ask_user_reminder=True AND message=None）
  4. COMPLETE_NEXT_STEP_REMINDER（場景 #2，被 #17 抑制，條件：next_step_reminder=True AND message=None AND NOT has_new_error_patterns）

**修改函式 3**: `main()` 函式（行 811-831）
- 步驟 6：將返回值解包為 5-tuple，並記錄 has_new_error_patterns 到日誌
- 步驟 8：傳遞新參數 has_new_error_patterns 和 new_error_pattern_files 給 generate_hook_output()

---

## Test Results

Phase 4c 多視角再審核完成（Reuse + Quality + Efficiency）。Quality：認知負擔從 23 降至 5（下降 78%），所有場景 #1/#2/#17 驗收條件達成，NamedTuple 正確實作，品質達標。Reuse：重構目標 100% 達成，_parse_ticket_date() 和 check_error_patterns_changed() 值得提取（已建 W22-024）。Efficiency：效率符合預期，ticket 檔案 2 次讀取為最主要優化點。技術債記錄：W22-021（frontmatter 統一）、W22-022（main 重構）、W22-023（AskUserQuestionMessages 評估）、W22-024（函式提取）。
### 驗證 1：Python 語法檢查
- hook_messages.py ✓ 通過 py_compile
- acceptance-gate-hook.py ✓ 通過 py_compile

### 驗證 2：訊息常數驗證
- ERROR_PATTERN_REMINDER 常數已正確定義 ✓
- 包含完整的三個選項說明和檔案清單佔位符 ✓

### 驗證 3：函式邏輯測試
使用直接的 Python 單元測試驗證：

**測試場景**：Ticket created = 2026-03-08, 新增檔案 mtime = 2026-03-08 20:00

**get_ticket_created_time() 測試**：
```
- 輸入：Ticket frontmatter 含 created: 2026-03-08
- 輸出：datetime(2026, 3, 8, 0, 0, 0) ✓
- 時間戳：1772899200.0
```

**check_error_patterns_changed() 測試**：
```
- 掃描目錄：.claude/error-patterns/（2 個檔案）
  - IMP-010.md (mtime: 1772856000.0 < ticket_ts) → 不計入
  - TEST-IMP-020.md (mtime: 1772971200.0 > ticket_ts) → 計入 ✓
- 返回值：(True, ['.claude/error-patterns/implementation/TEST-IMP-020.md'])
- 檔案路徑格式：相對路徑（相對於 project_root）✓
```

### 驗證 4：Hook 運行測試
- Hook 可執行，不出錯 ✓
- 接收 JSON 輸入 ✓
- 回應 JSON 輸出 ✓

### 驗證 5：無回歸風險確認
- 所有 check_acceptance_status() 返回點已更新為 5-tuple ✓
- generate_hook_output() 新參數有默認值（向後相容）✓
- 場景 #1 邏輯保留未被破壞 ✓（COMPLETE_REMINDER 仍在優先級 3）
- 場景 #2 被 #17 正確抑制 ✓（條件含 `AND NOT has_new_error_patterns`）

---

## Completion Info

**Completion Time**: (pending)
**Executing Agent**: pending
**Review Status**: pending