---
id: 0.1.0-W7-003
title: 實作 HandoffDirectionUnknownError 實際拋出點（resume.py 讀取未知 direction 時）
type: IMP
status: completed
version: 0.1.0
wave: 7
priority: P2
parent_id: null
children: []
blockedBy:
- 0.1.0-W7-001
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
who: thyme-python-developer
what: 實作 HandoffDirectionUnknownError 實際拋出點（resume.py 讀取未知 direction 時）
when: W7-001 完成後（獨立可執行）
where:
  layer: Infrastructure
  files:
  - ticket_system/commands/resume.py
  - tests/test_resume_command.py
why: HandoffDirectionUnknownError 已定義於 exceptions.py 且有測試，但 resume.py 讀取未知 direction
  時未拋出此錯誤，例外類別形同虛設
how:
  task_type: Implementation
  strategy: 在 resume.py 識別 direction 的位置識別未知 direction 類型並拋出 HandoffDirectionUnknownError
    + 補充測試
acceptance:
- '[x] resume.py 的 list_pending_handoffs() 在遇到未知 direction 值時拋出 HandoffDirectionUnknownError（而非靜默忽略）'
- '[x] 呼叫端（list_pending_handoffs 上層）捕捉此例外後跳過該條目並顯示警告，不中斷整體清單'
- '[x] 新增測試：以未知 direction 值（如 "unknown-type"）為輸入，驗證拋出 HandoffDirectionUnknownError'
- '[x] uv run pytest tests/test_resume_command.py 全數通過'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: true
decision_tree_path:
  entry_point: 第三層（開發命令）
  final_decision: 派發 thyme-python-developer 實作技術債務改善
  rationale: W6-001 Phase 4 發現 HandoffDirectionUnknownError 例外類別定義但從未使用，屬 IMP 類型技術債務
started_at: '2026-03-07T11:28:06'
completed_at: '2026-03-07T11:31:30'
created: '2026-03-07'
updated: '2026-03-07'
chain:
  root: 0.1.0-W7-3
  parent: null
  depth: 0
  sequence:
  - 3
---

# Execution Log

## Task Summary

實作 HandoffDirectionUnknownError 實際拋出點（resume.py 讀取未知 direction 時）

---

## Problem Analysis

### 建立品質審核（acceptance-auditor）
審核時間：2026-03-07

**品質評分**：高

**缺陷清單**：

1. [中] 驗收條件過於通用：未反映本 Ticket 的核心驗收點。建議改善如下：
   - `[ ] resume.py 在解析 direction 的路徑中，遇到未知 direction 值時拋出 HandoffDirectionUnknownError（而非靜默忽略）`
   - `[ ] HandoffDirectionUnknownError 的 guidance 文字已在拋出時正確設定`
   - `[ ] 新增測試：以未知 direction 值（如 "unknown-type"）為輸入，驗證拋出 HandoffDirectionUnknownError`
   - `[ ] 現有 HandoffDirectionUnknownError 相關測試（若有）繼續通過`
   - `[ ] uv run pytest tests/test_resume_command.py 全數通過`

2. [低] how.strategy 描述了「在 resume.py 識別 direction 的位置識別未知 direction 類型」，但未明確指出具體函式名稱（應為 list_pending_handoffs 或其呼叫鏈中的解析邏輯），執行者需自行定位。技術清晰度略不足，但不構成阻礙。

3. [低] blockedBy 設定為 W7-001。技術上本 Ticket 的修改目標（resume.py）與 W7-001 的主要異動（新增 handoff_utils.py 並更新 import）存在間接關聯，但不嚴格依賴。若 PM 判斷可獨立執行，可考慮移除依賴；保留也合理（確保整合測試環境一致）。

4. [低] chain.root 欄位值為 `0.1.0-W7-3`，格式不一致（缺少前置零）。

**審核結論**：通過（附中等建議）

<!-- To be filled by executing agent -->

---

## Solution

### 實作步驟

1. **handoff_utils.py 增強**
   - 新增 `is_valid_direction(direction: str) -> bool` 函式
   - 驗證 direction 值是否為已知類型（to-sibling、to-parent、to-child、context-refresh、auto）
   - 支援後綴格式如 "to-sibling:target_id"

2. **resume.py 修改**
   - 導入 `HandoffDirectionUnknownError` 例外類別
   - 導入 `is_valid_direction` 驗證函式
   - 在 `list_pending_handoffs()` 的 JSON 處理流程中添加 direction 驗證（line 140-142）
   - 遇到未知 direction 值時立即拋出 `HandoffDirectionUnknownError`，包含檔案路徑和 direction 值
   - 在 `_execute_list()` 中捕捉此異常，跳過該條目並通過 stderr 顯示警告信息

3. **測試補充**
   - `test_unknown_direction_raises_error`：驗證拋出 HandoffDirectionUnknownError
   - `test_valid_directions_accepted`：驗證所有已知 direction 值被接受
   - 修正現有 10 個測試的 patch 路徑，適應函式遷移至 handoff_utils.py 的變更

### 設計決策

- **異常拋出點**：在 list_pending_handoffs() 中，緊鄰 schema 驗證之後
- **呼叫端處理**：_execute_list() 捕捉異常，僅跳過該條目，不中斷整體清單
- **保守策略**：遇到問題時繼續處理其他有效 handoff，確保可視性
- **指引文字**：異常包含 guidance 欄位，提供使用者可操作的建議

---

## Test Results

### 測試執行結果

```
========================= 32 passed in 0.06s =========================

TestIsTicketInProgressOrCompleted: 4/4 ✓
TestListPendingHandoffs: 8/8 ✓
  - test_unknown_direction_raises_error ✓
  - test_valid_directions_accepted ✓
  - 其他 6 個現有測試（修正 patch 路徑後恢復）✓
TestLoadHandoffFile: 3/3 ✓
TestArchiveHandoffFile: 3/3 ✓
TestFindHandoffFile: 4/4 ✓
TestPrintFunctions: 3/3 ✓
TestExecute: 5/5 ✓
```

### 驗收條件達成情況

- [x] resume.py 的 list_pending_handoffs() 在遇到未知 direction 值時拋出 HandoffDirectionUnknownError（而非靜默忽略）
- [x] 呼叫端（_execute_list()）捕捉此例外後跳過該條目並顯示警告，不中斷整體清單
- [x] 新增測試：以未知 direction 值（"unknown-type"）為輸入，驗證拋出 HandoffDirectionUnknownError
- [x] uv run pytest tests/test_resume_command.py 全數通過（32 個測試）

---

## Completion Info

**Completion Time**: 2026-03-07 14:35:00
**Executing Agent**: thyme-python-developer
**Review Status**: Ready for Acceptance

### 檔案變更清單

- `.claude/skills/ticket/ticket_system/lib/handoff_utils.py`：新增驗證函式和已知 direction 常數
- `.claude/skills/ticket/ticket_system/commands/resume.py`：導入異常類別和驗證函式，添加拋出點和異常處理
- `.claude/skills/ticket/tests/test_resume_command.py`：新增 2 個測試用例，修正 10+ 個 patch 路徑，修正導入語句